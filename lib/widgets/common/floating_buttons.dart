import 'package:flutter/material.dart';

class FloatingButtonScanNSearch extends StatelessWidget {
  //const FloatingButtonScanNSearch({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(onPressed: () {});
  }
}
